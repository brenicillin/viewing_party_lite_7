<h1> Discover Movies </h2>
  <%= button_to "Back", discover_path, method: :get %>
<ul>
  <% @movies.each do |movie| %>
    <li>Movie Title: <%= link_to "#{movie.title}", movie_path(movie.id) %></li>
    <li>Rating: <%= movie.rating %></li> <br>
  <% end %>
</ul>